[defaults]
# Default to the repository root where active work happens.
workdir = "/home/todd/Dev/gm-kit"

# Shell used for commands.
shell = "bash"

# Allow writing inside the workspace without requesting escalation.
sandbox_mode = "workspace-write"

[projects."/home/todd/Dev/gm-kit"]
trust_level = "untrusted"

[notice]
"hide_gpt-5.1-codex-max_migration_prompt" = true
